#include<iostream>
#include<algorithm>
#define endl "\n"

using namespace std;
int a, b, v;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	freopen("input.txt", "r", stdin);
	cin >> a >> b >> v;
	
	int answer = 0;
	int left = 0;
	int right = v;
	int dist = a-b;
	while (left<=right){
		int mid = (left+right) / 2;
		}
	}

	
	cout << answer;
}
